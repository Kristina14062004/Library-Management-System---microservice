server:
  port: 8080

spring:
  application:
    name: fine-notification-service   # IMPORTANT for Eureka

  datasource:
    url: jdbc:mysql://localhost:3306/fine_notification_db
    username: root
    password: Mysql@2026!

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
